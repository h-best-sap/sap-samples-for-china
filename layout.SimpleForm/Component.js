sap.ui.define(["sap/ui/core/UIComponent"], function (UIComponent) {
  "use strict";

  let Component = UIComponent.extend(
    "sap.ui.layout.sample.SimpleForm354wide.Component",
    {
      metadata: {
        manifest: "json",
      },
    }
  );

  return Component;
});
